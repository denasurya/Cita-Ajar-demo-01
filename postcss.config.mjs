// File: postcss.config.mjs

const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};

// Menggunakan 'export default' agar sesuai dengan format file .mjs
export default config;
